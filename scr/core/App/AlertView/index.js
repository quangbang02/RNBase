/**
 * @format
 * @flow
 */

import React from 'react';
import AlertViewContainer from './AlertViewContainer';
import {type Props} from './types';

const AlertView = (props: Props) => {
  return <AlertViewContainer {...props} />;
};

export default AlertView;
