/**
 * @flow
 * @format
 */

import React from 'react';
import TitleContainer from './TitleContainer';
import {type Props} from './TitleView';

const Title = (props: Props) => {
  return <TitleContainer {...props} />;
};

export default Title;
